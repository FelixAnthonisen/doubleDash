package no.uib.inf101.doubleDash.model.tiles;

public class PowerupTile extends Tile {

    /**
     * /**
     * Call to create a new powerup tile. Calls the default constructor of
     * {@link Tile} with offset to match the powerup sprite in the tilemap
     */
    public PowerupTile() {
        super(0, 0);
    }
}